<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="XAutoSizeTextView">
        <!-- Specify the type of auto-size. Note that this feature is not supported by EditText, works only for TextView. -->
        <attr name="xAutoSizeTextType" format="enum">
            <!-- No auto-sizing (default). -->
            <enum name="none" value="0" />
            <!-- Uniform horizontal and vertical text size scaling to fit within the container. -->
            <enum name="uniform" value="1" />
        </attr>
        <!-- Specify the auto-size step size if <code>autoSizeTextType</code> is set to
        <code>uniform</code>. The default is 1px. Overwrites
        <code>autoSizePresetSizes</code> if set. -->
        <attr name="xAutoSizeStepGranularity" format="dimension" />
        <!-- Resource array of dimensions to be used in conjunction with
        <code>autoSizeTextType</code> set to <code>uniform</code>. Overrides
        <code>autoSizeStepGranularity</code> if set. -->
        <attr name="xAutoSizePresetSizes" format="reference"/>
        <!-- The minimum text size constraint to be used when auto-sizing text. -->
        <attr name="xAutoSizeMinTextSize" format="dimension" />
        <!-- The maximum text size constraint to be used when auto-sizing text. -->
        <attr name="xAutoSizeMaxTextSize" format="dimension" />
    </declare-styleable>
    
    <declare-styleable name="AdaptiveView">
        <attr name="adaptive_layout_width" format="dimension"/>
        <attr name="adaptive_layout_height" format="dimension"/>
        <attr name="adaptive_ratio" format="string"/>
        <attr name="adaptive_width_percent" format="float"/>
        <attr name="adaptive_vertical_bias" format="float"/>
        <attr name="adaptive_horizontal_bias" format="float"/>
    </declare-styleable>
</resources>